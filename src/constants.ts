import {LoginSource} from './types.d';

import type {LoginPages} from './types.d';

export const pages: LoginPages = {
	'chaos': {
		pageName: 'Chaos Game Visualiser',
		loginTarget: LoginSource.Window,
	},
};
